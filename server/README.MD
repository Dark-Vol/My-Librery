# My Library API Server

RESTful API сервер для управления библиотекой книг, построенный на Node.js, Express и Sequelize с MySQL.

## 📋 Содержание

- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [API Endpoints](#api-endpoints)
  - [Жанры (Genres)](#жанры-genres)
  - [Книги (Books)](#книги-books)
- [Модели данных](#модели-данных)
- [WebSocket](#websocket)
- [Примеры запросов](#примеры-запросов)

## 🚀 Установка

1. Установите зависимости:
```bash
npm install
```

2. Убедитесь, что у вас установлен и запущен MySQL сервер.

## ⚙️ Настройка

Настройте подключение к базе данных в файле `config/db.js`:

```javascript
const sequelize = new Sequelize(
    'mylibrary',    // имя базы данных
    'root',         // пользователь
    '',             // пароль
    {
        dialect: 'mysql',
        host: '127.0.0.1',
        port: 3306,
    }
);
```

Создайте базу данных MySQL:
```sql
CREATE DATABASE mylibrary;
```

## ▶️ Запуск

Запустите сервер:
```bash
npm start
```

Сервер запустится на порту **4000**.

Базовый URL API: `http://localhost:4000/api`

## 📡 API Endpoints

### Жанры (Genres)

#### Получить все жанры
```http
GET /api/genres
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "Фантастика",
    "createdAt": "2024-01-01T00:00:00.000Z",
    "updatedAt": "2024-01-01T00:00:00.000Z",
    "Books": []
  }
]
```

#### Получить жанр по ID
```http
GET /api/genres/:id
```

**Параметры:**
- `id` (path) - ID жанра

**Ответ:**
```json
{
  "id": 1,
  "name": "Фантастика",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "Books": []
}
```

**Ошибки:**
- `404` - Жанр не найден

#### Создать новый жанр
```http
POST /api/genres
```

**Тело запроса:**
```json
{
  "name": "Детектив"
}
```

**Ответ:** `201 Created`
```json
{
  "id": 2,
  "name": "Детектив",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

**Ошибки:**
- `400` - Название жанра обязательно

#### Обновить жанр
```http
PUT /api/genres/:id
```

**Параметры:**
- `id` (path) - ID жанра

**Тело запроса:**
```json
{
  "name": "Научная фантастика"
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Научная фантастика",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

**Ошибки:**
- `404` - Жанр не найден
- `400` - Название жанра обязательно

#### Удалить жанр
```http
DELETE /api/genres/:id
```

**Параметры:**
- `id` (path) - ID жанра

**Ответ:**
```json
{
  "message": "Жанр успешно удален"
}
```

**Ошибки:**
- `404` - Жанр не найден
- `400` - Невозможно удалить жанр. С ним связано N книг(и)

---

### Книги (Books)

#### Получить все книги
```http
GET /api/books
```

**Query параметры:**
- `genreId` (optional) - Фильтр по ID жанра
- `author` (optional) - Фильтр по автору
- `search` (optional) - Поиск по названию или автору

**Примеры:**
```http
GET /api/books?genreId=1
GET /api/books?author=Толстой
GET /api/books?search=война
```

**Ответ:**
```json
[
  {
    "id": 1,
    "title": "Война и мир",
    "author": "Лев Толстой",
    "genreId": 1,
    "bookSeries": null,
    "bookNumber": null,
    "aboutBook": "Роман-эпопея...",
    "createdAt": "2024-01-01T00:00:00.000Z",
    "updatedAt": "2024-01-01T00:00:00.000Z",
    "Genre": {
      "id": 1,
      "name": "Классика"
    }
  }
]
```

#### Получить книгу по ID
```http
GET /api/books/:id
```

**Параметры:**
- `id` (path) - ID книги

**Ответ:**
```json
{
  "id": 1,
  "title": "Война и мир",
  "author": "Лев Толстой",
  "genreId": 1,
  "bookSeries": null,
  "bookNumber": null,
  "aboutBook": "Роман-эпопея...",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "Genre": {
    "id": 1,
    "name": "Классика"
  }
}
```

**Ошибки:**
- `404` - Книга не найдена

#### Создать новую книгу
```http
POST /api/books
```

**Тело запроса:**
```json
{
  "title": "Преступление и наказание",
  "author": "Фёдор Достоевский",
  "genreId": 1,
  "bookSeries": null,
  "bookNumber": null,
  "aboutBook": "Роман о преступлении и его последствиях"
}
```

**Ответ:** `201 Created`
```json
{
  "id": 2,
  "title": "Преступление и наказание",
  "author": "Фёдор Достоевский",
  "genreId": 1,
  "bookSeries": null,
  "bookNumber": null,
  "aboutBook": "Роман о преступлении и его последствиях",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "Genre": {
    "id": 1,
    "name": "Классика"
  }
}
```

**Ошибки:**
- `400` - Название и автор обязательны
- `400` - Жанр не найден (если указан несуществующий genreId)

#### Обновить книгу
```http
PUT /api/books/:id
```

**Параметры:**
- `id` (path) - ID книги

**Тело запроса:** (все поля опциональны)
```json
{
  "title": "Новое название",
  "author": "Новый автор",
  "genreId": 2,
  "bookSeries": "Серия книг",
  "bookNumber": 1,
  "aboutBook": "Новое описание"
}
```

**Ответ:**
```json
{
  "id": 1,
  "title": "Новое название",
  "author": "Новый автор",
  "genreId": 2,
  "bookSeries": "Серия книг",
  "bookNumber": 1,
  "aboutBook": "Новое описание",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "Genre": {
    "id": 2,
    "name": "Детектив"
  }
}
```

**Ошибки:**
- `404` - Книга не найдена
- `400` - Жанр не найден (если указан несуществующий genreId)

#### Удалить книгу
```http
DELETE /api/books/:id
```

**Параметры:**
- `id` (path) - ID книги

**Ответ:**
```json
{
  "message": "Книга успешно удалена"
}
```

**Ошибки:**
- `404` - Книга не найдена

---

## 📊 Модели данных

### Genre (Жанр)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Первичный ключ (автоинкремент) |
| `name` | STRING(250) | Название жанра (обязательно) |
| `createdAt` | DATETIME | Дата создания |
| `updatedAt` | DATETIME | Дата обновления |

**Связи:**
- `hasMany` Book - один жанр может иметь много книг

### Book (Книга)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Первичный ключ (автоинкремент) |
| `title` | STRING(250) | Название книги (обязательно) |
| `author` | STRING(250) | Автор книги (обязательно) |
| `genreId` | INTEGER | ID жанра (внешний ключ, опционально) |
| `bookSeries` | STRING(250) | Название серии книг (опционально) |
| `bookNumber` | INTEGER | Номер книги в серии (опционально) |
| `aboutBook` | TEXT | Описание книги (опционально) |
| `createdAt` | DATETIME | Дата создания |
| `updatedAt` | DATETIME | Дата обновления |

**Связи:**
- `belongsTo` Genre - книга принадлежит одному жанру

---

## 🔌 WebSocket

Сервер поддерживает WebSocket соединения через Socket.IO для обмена сообщениями в реальном времени.

### События

#### Подключение к комнате
```javascript
socket.emit('joinRoom', numberRoom);
```

#### Отправка сообщения
```javascript
socket.emit('sendMessage', {
  room: numberRoom,
  text: 'Текст сообщения',
  role: 'user'
});
```

#### Получение сообщения
```javascript
socket.on('receiveMessage', (data) => {
  console.log(data);
});
```

---

## 💡 Примеры запросов

### cURL

#### Получить все книги
```bash
curl http://localhost:4000/api/books
```

#### Создать новый жанр
```bash
curl -X POST http://localhost:4000/api/genres \
  -H "Content-Type: application/json" \
  -d '{"name": "Фантастика"}'
```

#### Создать новую книгу
```bash
curl -X POST http://localhost:4000/api/books \
  -H "Content-Type: application/json" \
  -d '{
    "title": "1984",
    "author": "Джордж Оруэлл",
    "genreId": 1,
    "aboutBook": "Антиутопический роман"
  }'
```

#### Поиск книг
```bash
curl "http://localhost:4000/api/books?search=война"
```

### JavaScript (Fetch API)

```javascript
// Получить все книги
const books = await fetch('http://localhost:4000/api/books')
  .then(res => res.json());

// Создать новую книгу
const newBook = await fetch('http://localhost:4000/api/books', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    title: 'Название книги',
    author: 'Автор',
    genreId: 1
  })
}).then(res => res.json());

// Обновить книгу
const updatedBook = await fetch('http://localhost:4000/api/books/1', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    title: 'Обновленное название'
  })
}).then(res => res.json());

// Удалить книгу
await fetch('http://localhost:4000/api/books/1', {
  method: 'DELETE'
});
```

---

## 🛠️ Технологии

- **Node.js** - среда выполнения
- **Express** - веб-фреймворк
- **Sequelize** - ORM для работы с базой данных
- **MySQL** - база данных
- **Socket.IO** - WebSocket библиотека
- **CORS** - поддержка кросс-доменных запросов

---

## 📝 Структура проекта

```
server/
├── config/
│   └── db.js              # Конфигурация базы данных
├── controllers/
│   ├── booksControllers.js    # Контроллеры для книг
│   └── genresControllers.js   # Контроллеры для жанров
├── models/
│   └── models.js          # Модели Sequelize
├── routers/
│   ├── index.js           # Главный роутер
│   ├── booksRoutes.js     # Роуты для книг
│   └── genresRoutes.js    # Роуты для жанров
├── server.js              # Точка входа приложения
└── package.json           # Зависимости проекта
```

---

## ⚠️ Обработка ошибок

Все endpoints возвращают стандартные HTTP статус коды:

- `200` - Успешный запрос
- `201` - Ресурс успешно создан
- `400` - Ошибка валидации или некорректный запрос
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

Формат ответа с ошибкой:
```json
{
  "error": "Описание ошибки"
}
```

---

## 📄 Лицензия

ISC

