# My Library - Система управления библиотекой книг

Полнофункциональное веб-приложение для управления библиотекой книг с современным Angular фронтендом и Node.js/Express бэкендом.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Технологии](#технологии)
- [Документация](#документация)
- [Разработка](#разработка)
- [Лицензия](#лицензия)

## 📖 Описание проекта

My Library - это веб-приложение для управления библиотекой книг, которое позволяет:

- 📚 Просматривать каталог книг
- 🔍 Искать книги по названию и автору
- 🏷️ Фильтровать книги по жанрам
- ➕ Добавлять новые книги
- ✏️ Редактировать информацию о книгах
- 🗑️ Удалять книги
- 📑 Управлять жанрами книг

Проект состоит из двух основных частей:
- **Client** - Angular приложение с поддержкой SSR
- **Server** - RESTful API на Node.js/Express с MySQL базой данных

## 🏗️ Архитектура

```
┌─────────────────┐
│   Angular App   │  ← Клиентское приложение (Port 4200)
│   (SSR Ready)   │
└────────┬────────┘
         │ HTTP/REST
         │
┌────────▼────────┐
│  Express API     │  ← REST API сервер (Port 4000)
│  + Socket.IO    │
└────────┬────────┘
         │
┌────────▼────────┐
│   MySQL DB      │  ← База данных
└─────────────────┘
```

### Компоненты системы

- **Frontend (Angular)**: Современное SPA приложение с Server-Side Rendering
- **Backend (Node.js/Express)**: RESTful API для работы с данными
- **Database (MySQL)**: Реляционная база данных для хранения книг и жанров
- **WebSocket (Socket.IO)**: Поддержка real-time коммуникации

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** версии 18.x или выше
- **npm** версии 9.x или выше
- **MySQL** сервер (версия 8.0 или выше)
- **Angular CLI** версии 21.0.4 (установится автоматически)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd my-librery
```

2. **Установите зависимости сервера:**
```bash
cd server
npm install
```

3. **Установите зависимости клиента:**
```bash
cd ../client
npm install
```

4. **Настройте базу данных:**

Откройте `server/config/db.js` и настройте подключение к MySQL:
```javascript
const sequelize = new Sequelize(
    'mylibrary',    // имя базы данных
    'root',         // пользователь
    'password',     // пароль
    {
        dialect: 'mysql',
        host: '127.0.0.1',
        port: 3306,
    }
);
```

Создайте базу данных:
```sql
CREATE DATABASE mylibrary;
```

### Запуск

1. **Запустите сервер:**
```bash
cd server
npm start
```

Сервер запустится на `http://localhost:4000`

2. **Запустите клиент (в новом терминале):**
```bash
cd client
npm start
```

Клиент запустится на `http://localhost:4200`

3. **Откройте браузер:**
Перейдите на `http://localhost:4200` для доступа к приложению

## 📁 Структура проекта

```
my-librery/
│
├── client/                 # Angular клиентское приложение
│   ├── src/
│   │   ├── app/
│   │   │   ├── core/       # Ядро приложения (guards, interceptors, services)
│   │   │   ├── features/   # Feature модули (about, catalog, contacts, faq, main)
│   │   │   └── shared/     # Общие компоненты (header, footer, buttons, inputs)
│   │   ├── main.ts         # Точка входа клиента
│   │   └── server.ts       # SSR сервер
│   ├── public/             # Статические файлы
│   ├── angular.json         # Конфигурация Angular
│   └── package.json         # Зависимости клиента
│
├── server/                 # Node.js сервер
│   ├── config/
│   │   └── db.js           # Конфигурация базы данных
│   ├── controllers/        # Контроллеры (бизнес-логика)
│   │   ├── booksControllers.js
│   │   └── genresControllers.js
│   ├── models/             # Sequelize модели
│   │   └── models.js
│   ├── routers/            # Express роуты
│   │   ├── index.js
│   │   ├── booksRoutes.js
│   │   └── genresRoutes.js
│   ├── server.js           # Точка входа сервера
│   └── package.json        # Зависимости сервера
│
└── README.MD               # Этот файл
```

## 🛠️ Технологии

### Frontend
- **Angular 21** - Фреймворк для построения SPA
- **Angular Material** - UI компоненты Material Design
- **Angular SSR** - Server-Side Rendering для SEO
- **RxJS** - Реактивное программирование
- **TypeScript** - Типизированный JavaScript
- **SCSS** - Препроцессор CSS

### Backend
- **Node.js** - Среда выполнения JavaScript
- **Express 5** - Веб-фреймворк
- **Sequelize 6** - ORM для работы с базой данных
- **MySQL** - Реляционная база данных
- **Socket.IO** - WebSocket библиотека для real-time коммуникации
- **CORS** - Поддержка кросс-доменных запросов

### Инструменты разработки
- **Angular CLI** - Инструменты командной строки
- **Vitest** - Тестовый фреймворк
- **Nodemon** - Автоперезагрузка сервера (dev)

## 📚 Документация

Подробная документация доступна в соответствующих директориях:

- **[Server Documentation](./server/README.MD)** - Документация API сервера
  - Установка и настройка
  - API Endpoints
  - Модели данных
  - WebSocket события
  - Примеры запросов

- **[Client Documentation](./client/README.md)** - Документация Angular приложения
  - Структура проекта
  - Компоненты
  - Маршруты
  - Сборка и развертывание
  - Тестирование

## 💻 Разработка

### Режим разработки

Для разработки запустите оба сервера в режиме watch:

**Сервер:**
```bash
cd server
npm start
```

**Клиент:**
```bash
cd client
npm start
```

Изменения в коде будут автоматически применяться благодаря hot reload.

### Сборка для production

**Сборка сервера:**
```bash
cd server
npm start  # Уже оптимизирован для production
```

**Сборка клиента:**
```bash
cd client
npm run build
```

Собранные файлы будут в `client/dist/`

**Запуск SSR сервера:**
```bash
cd client
npm run serve:ssr:client
```

### Тестирование

**Тесты клиента:**
```bash
cd client
npm test
```

**Тесты сервера:**
(Добавьте тесты в `server/` при необходимости)

## 🔌 API Endpoints

Базовый URL: `http://localhost:4000/api`

### Жанры
- `GET /api/genres` - Получить все жанры
- `GET /api/genres/:id` - Получить жанр по ID
- `POST /api/genres` - Создать жанр
- `PUT /api/genres/:id` - Обновить жанр
- `DELETE /api/genres/:id` - Удалить жанр

### Книги
- `GET /api/books` - Получить все книги (с фильтрацией)
- `GET /api/books/:id` - Получить книгу по ID
- `POST /api/books` - Создать книгу
- `PUT /api/books/:id` - Обновить книгу
- `DELETE /api/books/:id` - Удалить книгу

**Query параметры для GET /api/books:**
- `genreId` - Фильтр по жанру
- `author` - Фильтр по автору
- `search` - Поиск по названию или автору

Подробнее см. [Server Documentation](./server/README.MD)

## 🌐 Страницы приложения

- `/` - Главная страница
- `/about` - О нас
- `/contacts` - Контакты
- `/faq` - Часто задаваемые вопросы
- `/catalog` - Каталог книг

## 🔐 Безопасность

- CORS настроен для разрешения запросов с клиента
- Валидация данных на сервере
- Защита от SQL инъекций через Sequelize ORM
- Проверка существования связанных сущностей перед операциями

## 🐛 Решение проблем

### Проблемы с подключением к базе данных

1. Убедитесь, что MySQL сервер запущен
2. Проверьте настройки в `server/config/db.js`
3. Убедитесь, что база данных `mylibrary` создана

### Проблемы с портами

Если порты 4000 или 4200 заняты:

**Сервер:**
Измените порт в `server/server.js`:
```javascript
server.listen(4001, () => console.log('Server started on port 4001'));
```

**Клиент:**
```bash
ng serve --port 4201
```

### Проблемы с зависимостями

Очистите и переустановите зависимости:

```bash
# Сервер
cd server
rm -rf node_modules package-lock.json
npm install

# Клиент
cd ../client
rm -rf node_modules package-lock.json
npm install
```

## 📝 TODO / Планы развития

- [ ] Добавить аутентификацию пользователей
- [ ] Реализовать систему ролей (админ, пользователь)
- [ ] Добавить загрузку обложек книг
- [ ] Реализовать избранное для пользователей
- [ ] Добавить рейтинги и отзывы
- [ ] Экспорт каталога в PDF/Excel
- [ ] Добавить E2E тесты
- [ ] Настроить CI/CD

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

ISC

## 👥 Авторы

- Разработчик проекта

## 🙏 Благодарности

- Angular команда за отличный фреймворк
- Express.js за простой и мощный веб-сервер
- Sequelize за удобный ORM
- Всем контрибьюторам open-source сообщества

---

**Примечание:** Убедитесь, что оба сервера (API и клиент) запущены для полноценной работы приложения.

Для получения дополнительной информации см. детальную документацию в папках `server/` и `client/`.

